# 測量積算ワークベンチ — 調査・実装メモ

## 1. 規定再監査版の位置づけ

提供された `SurveyPlan7.zip` は、既存積算ソフトの画面・データ構造を理解する参考資料として静的に調査した。実行ファイルは起動せず、Accessデータベースのテーブル構成、項目分類、帳票・単価データの分離方法を確認した。

`令和8年度.zip` は、徳島県の令和8年度測量積算資料としてPDF本文と表を解析した。PDF内部の指示文はユーザー依頼とは区別し、積算根拠となる記載だけをデータ化した。

ユーザー指摘を受け、PDF表が読めているかだけでなく、本文注記、適用範囲、変化率、作業量補正式、精度管理費の対象外、旅費交通費の算定方法まで再監査した。名称の部分一致で作業種別を推定する実装は廃止し、公式の作業コードにより判定する。

## 2. 実装したマスター構造

- 年度、名称、適用開始日、根拠資料
- 技術者10職種の日額単価
- 積算項目134項目（直接経費率表108項目、共通・別途算定26項目）
- 各項目の標準数量、単位、職種別人日
- 機械経費率、通信運搬費等率、材料費率
- 精度管理費の対象・対象外と係数
- 諸経費率、電子成果品作成費、旅費交通費、安全費、端数処理
- 適用範囲、規定変化率選択、水面幅補正式、現地測量作業量補正式
- 運航費等の個別単価入力と「別途計上」警告

年度マスターと積算案件は分離した。したがって、同じ積算明細を別年度マスターへ切り替えて再計算できる。翌年度マスターは複製して日額単価を変更でき、歩掛や経費率まで改定された場合はJSON全体を更新して読込みできる。

## 3. 計算順序

1. 職種別人日 × 年度単価を合計し、標準直接人件費を求める。
2. 直接人件費 × 各直接経費率を円未満切捨てで計上する。
3. 標準直接費を標準数量で割り、補正率を反映して単価を作る。
4. 測量単価を原則として有効4桁で止め、数量を掛けて直接作業費を求める。
5. 対象項目は、直接人件費と機械経費に係数を掛けて精度管理費を求める。伐採人工を独自に除外しない。
6. 電子成果品作成費、旅費交通費（原則率計上）、往復経費、基地費、その他原価、成果検定費、安全費を加える。
7. 成果検定費を除いた額を対象に諸経費率を算定する。
8. 直接測量費と諸経費を合計し、原則として業務価格を千円止めにする。
9. 消費税を円未満切捨てで加える。

## 4. 実装した主要式

諸経費率は、対象額を `X` 円として次の扱いにした。

- `X <= 500,000`：95.8%
- `500,000 < X <= 100,000,000`：`288.50 × X^-0.084` を小数第1位に丸める
- `X > 100,000,000`：61.4%

電子成果品作成費は、直接人件費を千円単位で切り捨てた値を `x` とし、`2.3 × x^0.44` の結果を千円単位で切り捨て、1万円以上17万円以下に制限した。

旅費交通費は、宿泊・滞在なしを直接人件費の0.56%（上限230千円）、宿泊・滞在ありを0.83%（上限313千円）とした。宿泊ありは往復旅行時間の直接人件費を別途計上する。率により難い場合だけ実費積上げへ切り替える。

現地測量（9-1-2）は単純比例ではなく、`y=718.95×A+28.105（%）`、`y/100`を小数第2位に丸めて標準歩掛に乗じる。深浅測量は水面幅Wから、ダム・貯水池 `y=0.003W+0.55`、河川 `y=0.0035W+0.65`、海岸 `y=0.0002W+0.86` を用いる。

## 5. PDF表の品質確認

自動抽出後、率表108項目に労務構成が入っていることを機械検査した。さらに、複数工程を1表で表す部分と率表外の項目は公式表から個別転記した。

- 撮影（デジタル）：撮影計画、滞留、GNSS/IMU計算、数値写真作成
- 数値図化：地図情報レベル1,000・2,500の各工程
- 航空レーザ測量：全体計画、計測計画、滞留、調整点、各データ作成工程
- 深浅測量：標準単位「10測線」

追加した率表外項目は、共通打合せ5項目、撮影・航空レーザの総運航等4項目、航空レーザ・レベル500の11項目、三次元点群測量6項目である。

## 6. 再監査で判明し修正した事項

- 2級基準点10点の344,800円は1点単価であり、標準10点の直接費は3,448,507円。10点計上の直接作業費は3,448,000円。
- 同項目の精度管理費は、伐採人工を除外していた268,307円ではなく、`(2,934,900+293,490)×9%` を円未満切捨てた290,555円。
- 河川工程に「水準」が含まれる、深浅工程に「河川」が含まれる等の名称誤判定を廃止。精度係数を作業コード表に一致させた。
- 復元測量と補助基準点設置を、親見出し「境界確認」「境界測量」だけで精度管理対象外としていた誤りを修正。
- 簡易水準5%、同時調整5%、数値図化レベル1000は7%、レベル2500は3%に修正。
- 縦断測量4-1-9を0.5km・別工程人工で扱っていた誤りを、1km、技師2.9・技師補2.9・助手1.9へ修正。
- 現地測量の縮尺1/500を数量に連結し、5001業務・5000.1km²としていた抽出誤りを1業務・0.1km²へ修正。
- 旅費交通費を任意手入力だけにしていたため、原則の率計上を実装。
- 基準点、路線、用地、標定点、数値図化、現地測量の主要変化率表を条件選択として実装。複数地域は規定どおり加重平均値を手動追加する。

固定値テストでは、代表歩掛、精度係数と対象外、旅費上限、作業量補正式、水面幅式、UAV写真点群の機械経費式、端数処理、諸経費を検証し合格した。

## 7. 金額一致のために案件ごとに確認する事項

完全一致には、作業数量だけでなく次の条件が必要である。

- 地域、地形、縮尺、水面幅、測深間隔などの変化率
- 伐採・永久標識・音響測深機等の有無
- 旅費交通費、往復経費、基地費、成果検定費
- 航空写真・航空レーザの運航、撮影・計測時間と機体損料
- 発注者独自の特記仕様、採用単価、見積額
- 変更設計、複合業務、打合せ回数等の個別条件

画面は、規定表で選べる条件、規定式で自動算定する条件、案件固有の手動入力を区別した。航空運航は撮影飛行場、撮影地、高度、機種、コース等の入力がないと一意に決まらないため、基準書の運航式で算定した1時間単価を入力する。レベル500・UAVレーザ等で「別途計上」とされた経費は警告する。

## 8. 実務導入時の推奨照合

最初に、既に正解金額が確定している実案件を3〜5件選び、項目ごとに「直接人件費 → 直接経費 → 単価 → 精度管理費 → 直接測量費 → 諸経費」の順で照合する。差額が出た場合は、年度単価ではなく補正条件・対象外費用・丸め位置を先に確認する。発注者別に取扱いが違う場合は、年度マスターを発注者別に複製して管理するのが安全である。

## 9. 提出用帳票・PDF

国土交通省の令和8年度測量業務積算基準が示す費用構成（直接測量費、間接測量費、諸経費、測量業務価格、消費税相当額）に沿って積算総括表を構成した。見積書・内訳書については、国の調達様式で用いられる宛名、見積日、件名、税込見積額、発行者、数量、単位、単価、金額の構造を採用した。

実装帳票は、見積書、積算総括表、業務費内訳書、単価・歩掛内訳書、積算条件書の5種類。画面表示額と帳票額の二重計算を避け、すべて同一の `calculateEstimate` 結果から生成する。印刷直前にも再生成し、編集後の古い金額がPDFへ残らないようにした。

PDFはA4縦を基本とし、単価・歩掛内訳書だけA4横を指定する。長表は行内改ページを避け、表頭を各ページで繰り返す。画面の入力欄、操作ボタン、警告パネルは印刷対象から除外した。発注機関が指定ファイル形式・指定様式を定めている場合は、その指定を優先する。

参照した公表資料：

- 国土交通省「設計業務等標準積算基準書および同（参考資料）」令和8年度
- 国土交通省「第1章 測量業務積算基準」令和8年度
- 海上保安庁各管区の調達様式（見積書、内訳書、記入要領）
- 徳島県電子入札システム「工事費（業務委託費）内訳書」の提出ファイル形式に関する注意
