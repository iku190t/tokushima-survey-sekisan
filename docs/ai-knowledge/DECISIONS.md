# 検証済みの設計判断

会話上の希望ではなく、現行コード、原資料照合記録、試験で確認できた判断だけを記録する。

## D-001 標準数量総額と1単位当り単価を分離する

- 状態: **検証済み**
- 判断: 標準歩掛の総額を、そのまま1点・1km等の単価として扱わない。標準数量で除し、所定の端数処理後に積算数量を掛ける。
- 代表例: 2級基準点測量・新点10点・伐採有りは、標準10点直接費3,448,507円、1点単価344,800円、10点直接作業費3,448,000円。
- 証拠: `規定再監査報告.md`、`調査・実装メモ.md`、`tests/test-engine.js`
- 昇格: プロジェクト判断。一般ルールへの追加確認回数は管理していない。

## D-002 精度管理費の対象判定は作業コードで行う

- 状態: **検証済み**
- 判断: 作業名の部分一致で精度管理対象を推定しない。公式作業コードと項目別設定を使用する。
- 理由: 「河川」「水準」など名称の重なりで誤分類が発生した。
- 証拠: `規定再監査報告.md`、`tests/test-engine.js`

## D-003 過去年度単価と完全過去年度マスターを区別する

- 状態: **検証済み**
- 判断: 令和4・5年度プリセットは技術者単価の公表値記録であり、完全過去年度マスターではない。比較用マスターを作る画面は廃止する。令和6～8年度は歩掛・率・技術者単価を同年度で組んだ「全国標準参考」と、原資料を地域別に照合した「県版検証済み」を明確に区別する。
- 廃止: 「令和8年度歩掛＋過去年度技術者単価」を利用者が作成する比較UI。正式年度と誤認しやすいため2026-08-22に廃止した。
- 証拠: `data/official-role-prices.js`、`app.js`、`index.html`、`tests/test-role-prices.js`、`tests/test-ui-static.js`

## D-004 数量精度は単位別に制限する

- 状態: **検証済み**
- 判断: 点・箇所・回等の離散量は整数、km・km²・時間等の連続量は小数第3位までとする。入力、貼付け、JSON読込みで同じ正規化を使う。
- 証拠: `engine.js`、`app.js`、`tests/test-engine.js`、`tests/test-ui-static.js`

## D-005 帳票金額は画面と同じ計算結果から生成する

- 状態: **検証済み（静的結線）**
- 判断: 見積書、総括表、業務費内訳書、単価・歩掛内訳書、積算条件書は `calculateEstimate` の結果から生成し、帳票専用の別計算を持たない。印刷直前にも再生成する。
- 証拠: `app.js`、`tests/test-ui-static.js`
- 未検証: 全帳票の実PDF目視品質。検証完了まではレイアウトの正常ベースラインへ昇格しない。

## D-006 起動時は新規画面とし、前回データは明示操作で復元する

- 状態: **検証済み（静的結線）**
- 判断: 起動時の `estimate` は必ず `emptyEstimate()` で作る。ローカル自動保存データは復元候補として保持し、「前回データを復元」を押した場合だけ表示する。
- 保護: 未操作で閉じる場合は空の新規データを保存せず、前回データを上書きしない。
- 区別: 「読込」はJSONファイルの読込み、「前回データを復元」はブラウザー内自動保存の復元とする。
- 証拠: `app.js`、`index.html`、`tests/test-ui-static.js`
- 未検証: 実ブラウザーでのローカルストレージ復元操作。手動確認が完了するまでは完全検証へ昇格しない。

## D-007 公開者・免責・応援・アクセス解析を画面上で明示する

- 状態: **検証済み（静的結線）**
- 判断: 公開画面のフッターは「Ez積算」ボタン1個だけにする。上部は大型の常設注意帯を置かず、「使い方・計算根拠」の右側に小型の「参考試算・免責」ボタンを置く。どちらを押しても、制作会社・公式サイト・OFUSE応援・ゲスト送信案内・免責・プライバシー説明を1つの統合ダイアログで表示する。`DISCLAIMER.md`、積算条件書と全帳票フッターの公式資料照合注意も維持する。
- 廃止: フッターに「制作会社」「公式サイト」「応援のご案内」「免責・プライバシー」を個別表示し、応援と免責を別ダイアログに分ける方式。加えて、本文上部を占有する大型の黄色い参考試算注意帯。導線の重複と表示の強さを抑えるため2026-08-22に廃止した。
- Analytics: GA4測定IDは既存Ezアイズ製品で公開使用中の`G-88B9YPJXWP`とし、公開HTTPS版ではページ表示時に読み込む。2026-08-23の依頼により同意ポップアップと設定変更UIは廃止した。広告保存・広告シグナルは無効にし、積算入力値・業務名・数量・PDF内容を送る独自イベントを実装しない。ローカルHTTP・`file:`実行では送信しない。
- 理由: 無料公開時の作成主体・非公式性・検証責任・外部送信を、利用前に利用者が確認できるようにするため。
- 証拠: `index.html`、`analytics.js`、`app.js`、`DISCLAIMER.md`、`tests/test-ui-static.js`
- ローカル検証: フッターの表示・ボタンが「Ez積算」1個、フッター直リンク0件、ダイアログ1個であること、統合ダイアログに制作・公式サイト・応援・免責・プライバシー設定が揃うこと、ブラウザー警告・エラー0件を確認した。
- 公開検証: GitHub Pagesビルド`1167136725`で、フッター「Ez積算」1ボタン、統合ダイアログの制作・公式サイト・応援・免責・プライバシー設定、ブラウザー警告・エラー0件を確認した。
- 未検証: 許可時のGA管理画面への着信、全端末での表示。

## D-008 全国対応は全国標準土台と県別検証済み上書きで行う

- 状態: **検証済み（基盤）**
- 判断: 積算地域と積算年度を別々に選択する。国交省公表の標準歩掛・技術者単価による令和6～8年度マスターを47都道府県共通の参考土台として選択可能にする。県の同年度原資料を照合済みの場合だけ県版検証済みマスターを優先する。
- 廃止: 完全マスター未収録県を選択不能にする方式。全国標準共通部分まで利用できなくなり、市販ソフトの標準単価セット型の使い方から離れるため、2026-08-22に廃止した。
- 配信: 公開版起動時に同一オリジンの `data/master-catalog.json` を確認する。`verified` の項目だけを対象にし、地域・年度・形式とSHA-256が一致したマスターだけをブラウザー内へ追加する。
- 将来年度: カタログに検証済み年度が追加された後は自動取得する。行政サイトから未監査データを直接取り込まない。
- 利用者マスター: 手動読込み・編集は許可するが `user-supplied` と明示し、検証済み扱いにしない。
- 表示: 県差分未確認時は「全国標準参考」とし、県独自歩掛、労務・材料・市場・機械単価、補正、適用通知が未反映であることを画面・提出前チェック・積算条件書に表示する。
- 証拠: `app.js`、`index.html`、`data/national-standard-masters.js`、`data/prefectures.js`、`data/master-catalog.json`、`tests/test-nationwide-standard.js`、`tests/test-master-catalog.js`、ローカルHTTP実ブラウザー確認。
- 未検証: 徳島県・広島県以外45都道府県の県独自差分。全国標準参考で計算可能であることを、全都道府県の金額検証完了と解釈しない。

## D-009 国直轄と都道府県を分離し、検証状態を段階表示する

- 状態: **検証済み（発注機関分離と表示）**
- 判断: 国土交通省（直轄）は47都道府県とは別の発注機関として選択する。令和6・7・8年度の国交省公開基準・技術者単価によるマスターは「全国標準参考」とし、地域原資料まで照合した「県版検証済み」と区別する。
- 用語変更: 旧表示「公開基準参照・要確認」は「全国標準参考」へ統一した。要確認事項は検証状態欄と提出前チェックに残す。
- 理由: 国土交通省自身が適用時期等を各発注機関（地方整備局等）へ確認するよう案内しており、各県も国準拠、県独自全編、県補足、市販版、CD貸出など運用が異なる。別発注機関の値を無条件に複製すると、未検証データを完全版と誤認させるため。
- 証拠: 国土交通省「設計業務等標準積算基準書および同（参考資料）」令和8年度欄、同「令和8年3月から適用する設計業務委託等技術者単価」、北海道・宮城県・山形県・徳島県の公式積算基準公開案内、`app.js`、`data/prefectures.js`、`tests/test-ui-static.js`、`tests/test-master-catalog.js`
- 検証範囲: 発注機関48件、全都道府県の令和6・7・8年度選択、国交省全国標準参考の選択、警告表示、公式出典リンク、県版検証済みの優先選択。
- 未検証: 地方整備局等の全運用、徳島県以外46都道府県の完全マスター、匿名化済み正解積算との金額一致。

## D-010 過去年度は歩掛・率・単価を同一年度で一体管理する

- 状態: **検証済み（広島県・令和6～8年度）**
- 判断: 過去年度は技術者単価だけを差し替えず、公式積算基準書本体・参考資料・技術者単価を年度単位で組み合わせた別マスターとして配信する。
- 年度差: 広島県の令和6年度諸経費率は91.2％／A=371.23／b=-0.107／51.7％、令和7・8年度は95.8％／A=288.50／b=-0.084／61.4％。直接経費率108行も年度ごとに別値で、令和8年度はUAVレーザ歩掛が改定されている。
- 収録: `r6-hiroshima-2024`、`r7-hiroshima-2025`、`r8-hiroshima-2026`。134項目、直接経費率108行、同年度技術者単価、旅費、安全費、電子成果品作成費を収録する。
- 配信: JSONカタログとローカル用JavaScriptバンドルを同じ生成物から作り、両者の完全一致とSHA-256を試験する。
- 証拠: 広島県公式の令和6・7・8年度積算基準書・参考資料、国土交通省の各年度技術者単価、`data/official-source-catalog.json`、`data/source-audits/hiroshima-r6-r8-expense-rates.json`、`tests/test-hiroshima-masters.js`、実ブラウザー確認。
- 限界: 航空運航費、成果検定費、案件固有条件、特記仕様、後日公表される正誤表は別途確認する。匿名化済み正解積算との全費目一致は未検証であり、公式発注者ソフト相当を保証しない。

## D-011 測量・設計等・地質一般は計算方式を分離して最後に合算する

- 状態: **検証済み（計算基盤・ローカルUI）**
- 判断: 測量、土木設計・調査計画・地質解析等、地質一般調査を同一計算式へ押し込まない。各区分の税抜業務価格を公式の構成に従って独立計算し、総合業務価格を合算して消費税を一度だけ計算する。
- 設計方式: 直接人件費と積上直接経費を分離し、その他原価=`直接人件費×0.35/(1-0.35)`、一般管理費等=`業務原価×0.35/(1-0.35)`とする。電子成果品作成費は概略・予備・詳細設計とその他設計を区別する。
- 地質一般方式: 直接調査費と間接調査費を対象額とし、100万円以下82.5%、100万円超3000万円以下`290.2×Y^-0.091`、3000万円超60.6%で諸経費を計算する。地盤情報データベース検定費等の対象外費用は別欄にする。
- 年度: 令和6～8年度の設計7職階と地質3職階は、各年度の国土交通省公表日額へ切り替える。
- 歩掛: 原資料で固定値確認した項目だけを「確認済み標準歩掛」として登録する。未確認作業は標準人工を推定せず、作業名・職種・人工を利用者が明示入力し、提出前警告を出す。
- 証拠: 国土交通省令和8年度土木設計業務等積算基準、地質調査積算基準、令和6～8年度設計業務委託等技術者単価、`consulting-engine.js`、`data/consulting-master.js`、`tests/test-consulting-engine.js`、実ブラウザー固定値確認。
- 未検証: 全設計・調査標準歩掛、市場単価、都道府県差分、実PDF全ページ、匿名化済み正解積算。

## D-012 PDF・OCR抽出値は確認候補として扱い、選択後だけ積算へ反映する

- 状態: **検証済み（匿名合成PDF・画像）**
- 判断: 文字情報を持つPDFは直接抽出し、文字を持たないPDFページと写真だけOCRを使用する。抽出結果を積算へ直結せず、原文、ページ番号、抽出方法、確信度、対応先、数量・人工を1つの確認画面に表示し、利用者が選択した項目だけ反映する。
- 入力規則: 測量数量は既存の単位別整数・小数規則、設計・調査・地質人工は小数第3位規則を反映時に再適用する。
- 保護: 低確信度候補と金額候補は初期選択しない。発注機関・年度は候補表示だけ行い、現在のマスターを自動変更しない。資料原文は確認中だけ保持し、保存JSONへ記録しない。
- OCR補正: 丸数字1～20、OCRで分離された日本語文字間隔、円金額の3桁点区切りを候補化前に正規化する。項目名称の誤認が残る場合は分類・単位一致の低確信度候補として残す。
- 証拠: `document-reader.js`、`document-import-engine.js`、`document-import.js`、`tests/test-document-import.js`、`tests/test-document-import-ui.js`、匿名合成PDF・同画像の実ブラウザー確認。
- 未検証: 実案件資料の多様な表組み、低解像度・傾き・手書き、全発注機関様式、OCR結果の正解率。

## D-013 業務基本情報も項目別の確認候補として扱う

- 状態: **検証済み（匿名合成原文・ローカルUI）**
- 判断: 業務名だけを特別扱いせず、発注者、担当部署、担当者、業務場所、履行期間、文書・業務番号、公告・資料日を構造化候補として抽出する。各候補は原文、ページ、抽出方法、確信度と並べ、その場で修正・選択した項目だけ保存データへ反映する。
- 反映先: 発注者は業務基本情報と帳票宛名、履行期間は業務基本情報と帳票納期へ同期する。基本情報は測量・設計・調査の共通情報として総括帳票にも表示する。
- 金額保護: 発注機関・年度マスターは計算結果へ影響するため初期選択しない。利用者が明示選択した場合だけ確認後に切替処理へ進む。資料からの検出だけで自動切替しない。
- 証拠: `document-import-engine.js`、`document-import.js`、`app.js`、`index.html`、`tests/test-document-import.js`、`tests/test-document-import-ui.js`、匿名合成原文の実ブラウザー反映確認。
- 未検証: 全発注機関の実様式、表セルが分離した資料、複数の発注者・期間・変更契約を含む資料、手書き・低品質OCRの正解率。

## D-014 公式案件検索は候補照合と根拠資料台帳に限定する

- 状態: **検証済み（公開API仕様・匿名合成XML・ローカルUI）**
- 判断: PDF・OCRから得た業務名、発注機関、業務・公告番号、年度、都道府県を官公需情報ポータルAPIの検索条件に使う。番号、業務名、発注機関、都道府県、年度、履行場所の一致理由と一致度を示すが、自動確定・自動積算反映はしない。
- 静的公開版: 公式API応答にブラウザーからの直接受信を許すCORSヘッダーを確認できないため、第三者中継を設けず、公式API検索XMLを保存して読み込む方式とする。検索条件は公式ポータルでも開けるようにする。
- 資料台帳: 公告、入札説明書、仕様書、数量表、金抜設計書、質問回答、訂正・差替えを区別し、「採用・要確認・不採用」を利用者が明示する。訂正候補が未採用なら警告する。URL、取得元、公開日、一致度、手元資料のSHA-256等だけを保存し、資料本文は保存JSONに入れない。
- 安全: XML・手動入力から生成するリンクはHTTP/HTTPSだけに限定し、手動登録はHTTPSを必須とする。公式らしいドメインか確認できない手動URLは警告する。
- 根拠: 官公需情報ポータル検索APIガイドV1.1、同ポータル利用案内、`official-case-engine.js`、`official-case-search.js`、`tests/test-official-case-engine.js`、`tests/test-official-case-ui.js`、匿名合成XMLの実ブラウザー確認。
- 未検証: 実案件での再現率・誤一致率、全公告の収録、リンクの継続、後日訂正の網羅、各発注機関サイトの版管理。

## D-015 主要業務を4タブへ分け、案件データと計算方式は共通管理する

- 状態: **検証済み（静的試験・ローカルUI）**
- 判断: 最初に表示する主要業務を「設計業務」「測量業務」「航空・船舶関係」「地質業務」の順にする。アプリ見出しは「web積算」とする。
- 区分: 設計業務は土木設計・調査計画、地質業務は地質解析等調査・地質一般調査を表示する。測量業務は共通・基準点・水準・路線・河川・用地・現地測量、航空・船舶関係は深浅・空中写真・航空レーザ・UAV写真点群・地上レーザ・UAVレーザを表示する。
- データ保護: タブは入力候補と内訳の表示を分けるが、同一案件の保存データを分割・移行しない。従来の計算エンジン、追加費用、総合結果、帳票を共通データから生成し、既存JSONとの互換性を維持する。
- 理由: 主要画面が測量専用に見えることと、設計・地質が一つの選択欄に混在することを避けつつ、同一案件の複合業務積算を保持するため。
- 証拠: `index.html`、`app.js`、`consulting.js`、`styles.css`、`tests/test-ui-static.js`、`tests/test-consulting-ui.js`、ローカル実ブラウザー確認。

## D-016 PDFクリック取込は反映待ちを経由して既存確認画面へ接続する

- 状態: **検証済み（文字入り匿名合成PDF）**
- 判断: PDF.jsで描画したページ画像へ、抽出した文字行座標に対応するクリック枠を重ねる。枠のクリックは積算を直接変更せず、業務基本情報・測量数量・設計等人工の反映待ち選択だけを切り替える。
- 確認: クリック選択後はD-012の確認・修正画面を必ず開き、対応先、数量、単位、人工、原文、ページ、確信度を再確認してから選択項目だけを反映する。
- 自動選択: 「確信度の高い候補を選択」は低確信度候補、金額へ影響する発注機関・年度を選ばない。PDF上でそれらを明示クリックした場合も、既存のマスター切替確認を省略しない。
- 保護: ページ画像と文字座標は確認中のDOMだけで保持し、保存JSONへ格納しない。読み込んだPDF自体を外部へ送信しない。
- 証拠: `document-reader.js`、`document-import.js`、`index.html`、`styles.css`、`tests/test-document-import.js`、`tests/test-document-import-ui.js`、匿名合成PDFのローカルHTTP実ブラウザー試験。
- 未検証: 全発注機関の実表組み、回転ページ、縦書き、複数段組、OCR画像PDFの実ブラウザー一連操作。OCR行座標は固定値試験だけを検証済みとする。

## D-017 PDF全行を手動対応付けでき、同一画面で連続反映する

- 状態: **検証済み（文字入り匿名合成PDF）**
- 判断: PDFページ上では、自動対応先を特定できた行だけでなく、抽出できた全文字行をクリック対象にする。未判定行をクリックした場合は右側に原文を示し、測量項目・数量、設計・調査・地質の業務区分・作業名・職種・人工、または業務基本情報の反映先を利用者が指定する。
- 反映: 右側の反映待ち内容を確認後、「積算へ追加」で反映する。反映後もPDF取込画面とスクロール対象を維持し、次の行を続けて選択できる。追加済みの候補は状態を保持し、同じ読込中の再クリックによる二重反映を防ぐ。
- 入力保護: 測量数量は選択したマスター項目の単位別規則で正規化し、点・回などは整数だけにする。設計等人工は0より大きい小数第3位までとする。未判定行は自動反映せず、反映先の明示指定を必須とする。「対象外」指定も可能にする。
- 詳細確認: 従来の一覧確認・修正画面は任意の詳細編集経路として残す。PDFから開いた場合は、その画面で反映してもPDF取込画面へ戻す。
- 廃止: D-016のうち「PDFクリック選択後は必ず別の確認ダイアログを開き、積算画面へ移動する」運用。右側で対応先・数量等を確認して明示反映するため、画面往復を必須にしない。
- 証拠: `document-import.js`、`index.html`、`styles.css`、`tests/test-document-import-ui.js`、匿名合成PDFのローカルHTTP実ブラウザー試験。
- 未検証: 全発注機関の表組みでの行分割精度、回転ページ、縦書き、複数段組、手書き、低品質OCR、1つの行から複数積算項目へ分割する操作。

## D-018 PDFの選択単位を文字ブロック・表セルとし、測量項目を分類から絞る

- 状態: **検証済み（固定値試験・公開PDF・公開UI）**
- 判断: PDFの同じ高さにある全文字を1行へ結合せず、横方向の空きを使って意味のある文字ブロック・表セルへ分ける。隣接した日本語字形は同じブロックへ結合し、離れた名称、工程、単位、数量、摘要は個別のクリック対象にする。
- 文脈: 個別ブロックを選択対象にする一方、同じ行の全文は`contextText`として数量・単位推定だけに使う。これにより名称セルを選んでも、同じ行の数量セルを候補値として参照できる。
- 手動対応: 測量の反映先は「分類」「詳細項目」の2段階にする。分類変更時に詳細項目を絞り、長い全項目一覧を直接探す操作を必須にしない。
- 画面整理: ファイル投下欄を高さ132pxへ縮小し、貼付け原文解析と公式案件検索・資料台帳の画面・スクリプト読込を廃止する。旧保存JSONとの互換維持に必要な案件データ構造と内部コードは残す。
- 証拠: `document-reader.js`、`document-import.js`、`index.html`、`styles.css`、`tests/test-document-import.js`、`tests/test-document-import-ui.js`、`tests/test-official-case-ui.js`。固定値で同一行5セルとOCR3セルの分割を確認し、国土地理院公開PDF7ページで290ブロック、同一表行最大10ブロック、公開版の分類絞込みとブラウザー警告・エラー0件を確認した。
- 未検証: PDF内部で1行全体が単一文字オブジェクトになっている資料、文字座標が壊れたPDF、回転・縦書き・複雑なセル結合、単語座標を返さないOCR結果。これらは完全自動分割を保証しない。

## D-019 PDFの項目セルと数量セルを別々にドラッグして1候補へまとめる

- 状態: **検証済み（固定値試験・実ブラウザー）**
- 判断: PDF文字ブロックをクリック可能なままドラッグ元にもする。右側には常時見える「項目」「数量」の受け皿を置き、項目名セルと数量セルを別々に落として1つの手動候補へ関連付ける。詳細項目・数量の入力欄自体もドロップ先にする。
- 項目照合: ドロップ文字とマスターの分類・項目名を正規化して照合する。一意一致だけを自動選択し、同じ語に複数項目が一致する場合は分類まで絞って利用者の詳細選択を要求する。
- 数量保護: 数量セルの文字から候補値を入れるが、積算へ追加する時点で選択項目の単位別整数・小数規則を再適用する。項目セルと数量セルの原文は右側で別々に表示する。
- 操作継続: 最初のドロップ後にPDFを自動スクロールしない。標準HTMLドラッグに加え、マウス・ペン・タッチのポインター移動先を判定する経路を備え、ドラッグ後にクリック処理を重ねて実行しない。
- 証拠: `document-import.js`、`index.html`、`styles.css`、`tests/test-document-import-ui.js`。公開PDF7ページをローカル実画面へ読み込み、項目セルと数量セルを連続ドラッグして右側へ別々に表示し、PDF座標が最初のドロップ前後で不変、ブラウザー警告・エラー0件を確認した。
- 未検証: すべての実案件名称に対する項目自動一致率、モバイル実機での長距離ドラッグ、回転・縦書き資料。自動一致しない場合は分類・詳細項目を手動選択する。
- 廃止: 常時見える別置きの「項目」「数量」受け皿は、実入力欄と重複するためD-021で廃止した。ドラッグ操作と原文関連付け自体は実入力欄へ移して維持する。

## D-020 追加済み項目をその場で変更し、公開版アクセス解析は常時計測する

- 状態: **検証済み（静的試験・ローカル実ブラウザー・公開実ブラウザー）**
- 画面名: 資料取込のナビゲーションと見出しを「PDF・写真から取込み」に統一する。
- PDF反映待ち: 追加した測量数量、設計等人工、通常基本情報は反映待ち一覧自体をボタンにする。押すと右側の対応付け欄へ現在値を再表示し、「変更を保存」で同じ候補オブジェクトを更新する。新規候補を追加しないため、変更前後で反映待ち件数を増やさない。費用候補は従来の「一覧で詳しく修正」で変更する。
- 積算内訳: 反映済み測量項目は内訳表の作業項目選択欄から変更する。数量は変更先単位の規則で正規化し、旧項目固有の補正率、補正選択、条件値、精度係数、手動単価を新項目の初期値へ戻す。
- Analytics: 同意ポップアップと設定変更UIを廃止し、公開HTTPS版だけGA4ページビューを常時計測する。広告用保存と広告シグナルは拒否し、積算項目名、数量、業務名、PDF・写真内容を送る独自イベントは追加しない。
- 証拠: `index.html`、`app.js`、`document-import.js`、`analytics.js`、`styles.css`、`tests/test-ui-static.js`、`tests/test-document-import-ui.js`。国土地理院公開PDFを使ったローカル実ブラウザーで反映待ち1件を変更後も1件のまま保持し、公開版で積算項目変更、GAタグ読込み、同意ポップアップなし、ブラウザー警告・エラー0件を確認した。
- 未検証: GA4管理画面への着信、費用候補の右側インライン変更、設計等の積算内訳へ反映済み行を同じ方法で別作業へ変更する操作。

## D-021 資料上の数量と単位を保持し、積算基準単位へ換算する

- 状態: **検証済み（固定値試験・ローカル実ブラウザー・公開表示）**
- 画面: 別置きの「PDFからドラッグして入力」欄を廃止し、実際に確定する「積算する詳細項目」「資料の数量」「資料の単位」を常時見える主ドロップ先にする。PDFの項目名、数量セル、単位セルを別々に落として1候補へ関連付ける。
- 単位換算: 資料数量と資料単位を保持したまま、選択したマスター項目の単位へ換算してから`SekisanEngine.normalizeQuantity`を適用する。m²項目では標準10,000m²、m²、ha、km²、km²項目では標準数量、km²、ha、m²、距離ではm・kmを選択できる。
- 代表固定値: 用地測量「公図等の転写」は標準10,000m²当りであるため、資料数量6.9・資料単位10,000m²を69,000m²へ換算して積算内訳へ反映する。10,000m²を数量そのものとして扱わず、後続の6.9を資料数量として保持する。
- 保護: 換算式と積算へ反映する実数量を保存前に表示する。縮尺数値を数量へ連結したI-004を再発させず、単位表記と数量を分離して固定値試験する。
- 証拠: `document-import-engine.js`、`document-import.js`、`index.html`、`styles.css`、`tests/test-document-import.js`、`tests/test-document-import-ui.js`。固定値試験で6.9×10,000m²＝69,000m²、ローカル実ブラウザーで換算表示・反映待ち表示・積算内訳69,000m²、公開版で新UIとキャッシュ版、ブラウザー警告・エラー0件を確認した。
- 未検証: 全発注機関の単位表記ゆれ、尺・ヤード等の非メートル単位、1セル内に複数の数量と単位がある資料。未収録単位は自動確定しない。

## D-022 PDF入力欄をプレビュー上端へ固定し、反映待ちは下へ分離する

- 状態: **検証済み（静的試験・公開PDFのローカル実ブラウザー・公開表示）**
- 判断: PDF右側の固定列には「PDFから項目・数量・単位を入れる」入力欄だけを置く。反映待ち件数、候補一覧、反映操作はPDF左右レイアウトの下に独立した横長領域として置き、候補件数によって入力欄の開始位置を変えない。
- 固定: デスクトップでは右側入力欄を`position: sticky`、PDF表示と同じ上端、PDF表示と同じ最大高さにする。狭い画面では1列表示へ戻し、固定と高さ制限を解除する。
- 証拠: `index.html`、`styles.css`、`tests/test-document-import-ui.js`、アプリコミット`a0d3c7b11b32e74af5f2112cab34432729ebf20f`。国土地理院公開PDFをローカル実画面へ読み込み、反映待ち1件から4件まで追加後もPDFと入力欄の上端差0px、反映待ち領域がPDFレイアウト下、ブラウザー警告・エラー0件を確認した。GitHub Pagesビルド`1168990713`で新DOM配置と`styles.css?v=20260823-4`を確認した。
- 未検証: スマートフォン実機、画面高さが極端に低い端末、ブラウザー拡大率200%以上での長時間連続操作。
