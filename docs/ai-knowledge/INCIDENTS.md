# 過去の不具合と修正理由

この文書は、再発防止に必要な検証済み事例だけを記録する。コミット履歴がないため、発生版・修正版のコミットIDは不明である。

## I-2026-08-25-FISCAL-MIX-01 年度切替後に旧年度歩掛と新年度単価が混在した

- 状態: **検証済み・修正済み**
- 日付: 2026-08-25
- 対象: 修正コミット `b16da33ccd43283d756b689913c07c10eabfe7b9`
- 症状: 測量マスターの年度を切り替えても、設計等の年度選択と既存標準歩掛行を独立して保持でき、旧年度歩掛に新年度の技術者単価を掛けられた。
- 再現: 異なる年度の標準歩掛行を保存した後に上部の年度を切り替え、設計等の集計を実行する。
- 原因: 測量と設計等が別の年度状態を持ち、標準歩掛行の年度一致を計算時に検証していなかった。
- 修正: 年度選択を4業務で同期し、年度不一致行を計算対象外にした。測量の旧年度補正条件は切替先マスターの初期状態へ戻す。
- 検証: 年度同期・不一致遮断・保存復元の回帰試験、全19試験、測量3年度の同一コードに単位・標準数量差分0件を確認した。
- 再発防止: 年度は案件共通状態とし、行側の`fiscalYear`一致を金額化の必須条件にする。

## I-2026-08-25-COVERAGE-GATE-01 人工表照合だけで完全確認済みとして計算した

- 状態: **検証済み・修正済み**
- 日付: 2026-08-25
- 対象: 修正コミット `b16da33ccd43283d756b689913c07c10eabfe7b9`
- 症状: 補正、条件表、数量式等が未構造化の標準歩掛候補も「原表確認済み」として追加・計算できた。
- 再現: `verificationStatus`が未設定または人工表照合だけの行を選択し、条件未実装のまま積算へ追加する。
- 原因: 完全性判定の既定値が許可側で、行が持つ未解決規則を計算可否に使用していなかった。
- 修正: 既定拒否へ変更し、構造化条件または未解決規則なしを確認できた195行だけ計算可、参照専用32行と未構造化規則あり1,166行は計算対象外にした。
- 検証: 不明状態、構造化条件、参照専用、未解決条件を分けた回帰試験と全19試験が合格した。
- 再発防止: 取得済み、原表照合済み、条件構造化済み、計算検証済みを別状態で保存し、前段階を後段階へ自動昇格させない。

## I-001 344,800円を標準10点の総額として扱った

- 状態: **検証済み・修正済み**
- 症状: 2級基準点測量の344,800円を10点総額と誤認できる表示・計算だった。
- 原因: 標準歩掛の数量単位と、1点当り単価の区別が不足していた。
- 正しい値: 標準10点直接費3,448,507円、1点単価344,800円、10点直接作業費3,448,000円。
- 再発防止: 標準数量、標準総額、1単位単価、積算数量総額を別項目で表示・試験する。
- 証拠: `規定再監査報告.md`、`tests/test-engine.js`

## I-002 基準点測量の精度管理費から伐採人工を誤って除外した

- 状態: **検証済み・修正済み**
- 症状: 精度管理費を268,307円としていた。
- 原因: 路線測量の独立工程「伐採」の対象外規定を、基準点測量の標準歩掛内の伐採人工へ誤適用した。
- 正しい値: `(2,934,900 + 293,490) × 9%` の円未満切捨てで290,555円。
- 再発防止: 別工程の注記を名称だけで横展開せず、作業コードと表の適用範囲を照合する。
- 証拠: `規定再監査報告.md`、`tests/test-engine.js`

## I-003 名称の部分一致で精度係数を誤分類した

- 状態: **検証済み・修正済み**
- 症状: 河川、深浅、用地、復元測量等で精度管理係数または対象外判定が誤った。
- 原因: 作業名に含まれる「河川」「水準」等の語から区分を推定した。
- 修正: 公式作業コードを主キーにした項目別判定へ変更した。
- 再発防止: 名称推定ロジックを復活させない。新項目はコードと原表で追加する。
- 証拠: `規定再監査報告.md`、`調査・実装メモ.md`

## I-004 PDF表抽出で縮尺文字列を数量へ連結した

- 状態: **検証済み・修正済み**
- 症状: 現地測量の数量を5001業務、5000.1km²として扱った。
- 原因: 縮尺1/500の文字列と数量セルを機械抽出時に連結した。
- 修正: 1業務、0.1km²へ修正した。
- 再発防止: PDF抽出値は、単位・標準数量・前後見出しを含めて原表と照合する。
- 証拠: `規定再監査報告.md`、`調査・実装メモ.md`

## I-005 旅費交通費を任意手入力だけにしていた

- 状態: **検証済み・修正済み**
- 症状: 原則の旅費率計上が自動反映されなかった。
- 修正: 宿泊なし0.56%・上限23万円、宿泊あり0.83%・上限31.3万円、実費積上げを切替可能にした。
- 再発防止: 手入力を既定値にせず、基準の原則式と例外入力を分ける。
- 証拠: `engine.js`、`規定再監査報告.md`、`tests/test-engine.js`

## I-006 起動時に前回の積算を自動表示した

- 状態: **再現済み・修正済み・静的試験済み**
- 症状: ページを開くとローカル保存された前回の積算が、そのまま現在の入力画面に表示された。
- 原因: 初期化時に `estimate = loadEstimate()` を実行し、起動直後にも無条件で `persistEstimate()` を実行していた。
- 修正: 入力画面は `emptyEstimate()` で開始し、保存データは復元候補として別保持する。復元ボタンを押した場合だけ読み込む。未操作終了時の無条件保存を廃止した。
- 再発防止: T-UIで新規初期化、明示復元、未操作時の上書き防止を静的検査する。
- 証拠: 修正前後の`app.js`確認、`tests/test-ui-static.js`
- 未検証: 実ブラウザーを使った操作試験。

## I-007 スマートフォン幅で積算明細表がページ全体を横へ押し出した

- 状態: **再現済み・緩和済み・実ブラウザー部分検証済み**
- 症状: 公開HTTPS版を幅390pxで表示すると、ページの`scrollWidth`が510pxとなり、積算明細表のために画面全体が横スクロールした。
- 原因: 積算表自体には横スクロール枠があったが、CSS Grid項目の`.workspace-main`に`min-width: 0`がなく、表の最小内容幅まで親グリッドが縮まなかった。
- 修正: `.workspace-main, .master-layout`へ`min-width: 0`を追加し、広い表は`.table-scroll`内だけで横スクロールさせる。
- 検証結果: 公開版の幅390pxで、本文幅375px、積算表の表示枠345px・内部内容505px、タブ2段折返しを確認した。表は専用枠内で横スクロールする。自動計測上の`documentElement.scrollWidth`は485pxを報告しており、全ブラウザーでページ根元の横移動が完全に解消したとは確定していない。
- 再発防止: T-UIで親グリッドの最小幅解除、表のレイアウト封じ込め、タブ折返しを静的検査する。別ブラウザーの実機確認が完了するまで「完全修正」へ昇格しない。
- 証拠: 公開版の幅390px実測、`styles.css`、`tests/test-ui-static.js`

補足: 修正直後の公開確認ではブラウザーが旧`styles.css`を再利用したため、HTMLからCSS・JavaScriptを版番号付きURLで参照し、公開更新時のキャッシュ混在を防止した。

## I-012 国交省の年度改定だけを主に照合し、基準書本体と未入力値を安全に扱えていなかった

- 状態: **再現済み・修正済み（全リンク取得監査・固定値試験・実ブラウザー検証待ち）**
- 症状: 設計等の年度別歩掛へ初期倍率1を適用でき、PDF取込の数量も数値が見つからない場合に1を補っていた。また、出典一覧が年度改定中心で、平成23年度の第1～4編と参考資料の継続適用部分を明示していなかった。
- 原因: 「資料を取得した状態」「原表の職種別人工を抽出した状態」「数量補正式まで実装した状態」を区別せず、直近年度の改定資料を中心に扱っていた。
- 修正: ページ内171リンクと重複除外PDF152件を台帳化・取得し、基準書本体と参考資料を出典へ追加した。補正係数、人工、PDF数量・人工・単位を空欄開始にし、原表で補正係数を算定しない限り標準歩掛を追加しない。数量・人工と補正係数の端数桁を総則に合わせた。
- 再発防止: 年度更新は本体＋過年度改定＋当年度改定の累積監査とし、資料取得件数を計算実装件数として表示しない。空欄を1または基準単位へ暗黙変換しない。
- 証拠: D-031の監査台帳、計算・UI変更、T-CATALOG、T-CONSULT、T-DOC-IMPORT、T-UI。

## I-008 資料取込の正規化で小数点を削除した

- 状態: **再現済み・修正済み・固定値試験済み**
- 症状: 初期実装で「技師（A） 2.5人日」を25人日として候補化した。
- 原因: 名称照合用の空白・記号除去処理で小数点まで削除し、その文字列を人工の数値抽出にも使っていた。
- 修正: 名称照合用の正規化でも小数点を保持し、2.5、0.75、1.25人日の固定値試験を追加した。
- 再発防止: OCR・PDFの正規化変更時はT-DOC-IMPORTで整数、少数、縮尺、丸数字を同時検査する。
- 証拠: `document-import-engine.js`、`tests/test-document-import.js`

## I-009 資料取込0件でも選択を要求した

- 状態: **再現済み・修正済み・実ブラウザー検証済み**
- 症状: 業務基本情報0件・積算候補0件の確認画面にも全選択チェックと無効な「反映する項目を選択してください」ボタンが表示され、操作方法が分からない状態になった。
- 原因: 選択件数だけで反映ボタンを無効化し、選択可能な行が存在するかを画面状態へ反映していなかった。ラベルのない総括表見出しも業務名候補の対象外だった。
- 修正: 0件の区分はパネルと全選択操作を隠す。全区分0件では明示案内と、選択不要で閉じられるボタンを表示する。積算0件・基本情報ありの場合は基本情報だけ反映できる。総括表のラベルなし見出しは年度・業務分野語・末尾語を満たす場合だけ要確認の業務名候補にする。
- 再発防止: T-DOC-IMPORTへ匿名合成の総括表見出しと「式1を詳細数量へ誤対応しない」固定値を追加し、T-DOC-UIへ0件時の終了動作と選択欄非表示の結線検査を追加した。
- 証拠: `document-import-engine.js`、`document-import.js`、`tests/test-document-import.js`、`tests/test-document-import-ui.js`、ローカルHTTP版・GitHub Pages版の実ブラウザー確認。

## I-010 PDF表の横一列を1つの選択枠へ結合した

- 状態: **再現済み・修正済み・固定値／実ブラウザー検証済み**
- 症状: 業務数量総括表の「用地測量」「打合せ」「業務着手時」「回」「1」のように、同じ横行にある別セルが1つのクリック枠へ結合され、個別項目を選べなかった。
- 原因: PDF文字抽出処理がY座標の近い文字をすべて1行へまとめ、横方向の空きとセル境界を選択単位に使っていなかった。
- 修正: 同じ行を横方向の空きで文字ブロックへ分割し、隣接字形は結合する。各ブロックに同じ行の全文`contextText`を付与し、選択は個別、数量推定は行文脈で行う。OCRも単語境界座標がある場合は同じ規則でセル分割する。
- 再発防止: T-DOC-IMPORTで5セル分割、隣接日本語結合、OCR3セル分割を固定値検査する。公開PDFの実ブラウザー読込でもページ数、総ブロック数、同一行の複数ブロックを確認する。
- 証拠: `document-reader.js`、`tests/test-document-import.js`、国土地理院公開PDF7ページの実ブラウザー確認（290ブロック、表行最大10ブロック）。

## I-011 測量項目の規定書リンク要求を全業務の資料台帳へ拡大解釈した

- 状態: **再現済み・修正済み・静的／実ブラウザー検証済み**
- 症状: 測量項目に使用規定書名・PDFリンク・該当ページがないという問題に対し、設計・調査計画・地質の採用歩掛表や全年度資料台帳まで追加し、求められた測量項目直下の表示が残った。
- 原因: 「PDFの一覧表」を全業務の原資料台帳要求と解釈し、元の不足箇所である`selectedItemMeta`がページ番号だけを表示して規定書名・URLを結線していない事実へ絞らなかった。
- 修正: 設計等へ追加した表を撤回し、測量項目選択直下へ4用途の規定書・PDF表を追加した。年度一覧も全国標準測量マスターの`sourceLinks`だけへ限定した。
- 再発防止: 出典表示の不足は、最初に「どの業務区分」「どの画面位置」「資料名・URL・ページのどれが欠けるか」を実DOMとデータ構造で特定する。別業務へ横展開する前に対象範囲を固定する。
- 証拠: 修正前コミット`972e9ff862d5ceb07b4c70675760d73cfbeac9f0`、`app.js`、`index.html`、`tests/test-ui-static.js`、令和8年度2級基準点測量のローカル実ブラウザー確認。

## 廃止された考え方

- 状態: **廃止**
- 「作業名の文字列から精度管理区分を推定する」方式。
- 「過去年度技術者単価を入れ替えれば完全な過去年度積算になる」という扱い。
- 「PDFから抽出できた数値は原表照合なしで確定値にできる」という扱い。

## I-013 全国標準リポジトリへ特定県の検証資料を残した

- 状態: **再現済み・修正済み（自動試験・公開確認待ち）**
- 症状: 通常画面では全国標準だけを表示していたが、リポジトリには特定県の3年度マスター、原資料台帳、生成スクリプト、試験が残り、全国標準参考歩掛の出典URL・ページもその県の公開全編を指していた。
- 原因: 全国標準の数値土台を作る際に使った県版全編を、過去の検証証跡として現行リポジトリへ残したため、現在の全国標準方針とデータ構成が一致しなかった。
- 修正: 県別固有ファイルと作業PDFを削除し、公式原資料台帳から県別エントリを除外した。全国標準の表示出典を国交省資料だけに限定し、県版ページ番号を削除して「現行全編の項目別ページ未対応」とした。
- 再発防止: T-CATALOGで県別管轄コードと都道府県調達URLが0件であること、T-NATIONALとT-CONSULT-WALKSで原表ページを流用しないことを固定値検査する。

## I-014 設計・調査・地質の条件表を職種別人工だけへ平坦化した

- 状態: **再現済み・修正済み・生成監査／固定値／実ブラウザー検証済み**
- 症状: 測量以外の通常入力が職種・人工中心で、業務量、橋長、径間、形式、ボーリング孔数、運搬距離等の条件を選ばなくても人工を追加できた。編成人員表も通常歩掛と同様に数量比例できた。
- 原因: 旧生成処理がPDF表から職種別人工と直前の標準単位を抽出した一方、表の周囲にある適用範囲、数量算定式、補正、追加控除、規格区分、日当たり作業量との関係をデータ化しなかった。その欠落を利用者入力の補正係数・人工で補う設計を通常経路にしてしまった。
- 修正: 通常経路を業務種類・適用条件・数量入力へ変更し、国交省ページへ逆照合した1,393歩掛行へ置換した。適用範囲、補正式、条件表、日当たり作業量を表示・選択し、道路数量式、測量方式調査計画、地質市場単価を分離した。人工直接入力は折りたたみの手動調整へ移した。
- 再発防止: 表の数や人工セルの抽出件数を計算実装件数とみなさない。少なくとも「適用条件」「独立変数」「数量式」「補正・加算控除」「費目構成」「端数」「原表ページ」を揃え、正解案件で一致するまで完全実装へ昇格しない。
- 証拠: 修正前の`tools/generate-consulting-walks.py`履歴、国交省基準書本文、D-033、T-CONSULT-WALKSの703表／32表分類、ローカル実ブラウザー。

## I-015 資料取得・人工表抽出を「全部読み込み・全部実装」と説明した

- 状態: **再現済み・修正済み・生成監査／固定値／実ブラウザー検証済み**
- 症状: 735表の職種別人工を収録した段階で、周辺の適用範囲、補正式、追加・控除、数量算出、参照表の組合せが未実装なのに、全資料・全表を実装した趣旨の説明をした。
- 原因: PDF取得件数、人工表抽出件数、計算可能な規則件数を同じ「収録」として扱い、ページ単位の規則監査と完全性ゲートがなかった。
- 修正: 現行系列124文書・2,102ページを索引化した上で、年度統合表777対象ページを国交省本体・累積改定へ逆照合した。計算対象1,393行は全て国交省URLとページを持ち、条件表・数式・適用注記を画面へ結線した。単なるPDF取得数と計算実装数を分けて表示する。
- 再発防止: 「取得済み」「ページ索引済み」「人工表抽出済み」「条件規則実装済み」「正解案件一致」を別状態にする。最後の2状態を満たさない業務を完全実装と説明しない。
- 証拠: D-034、`data/source-audits/mlit-effective-rule-pages.json`、`data/consulting-condition-rules.js`、T-CONSULT-CONDITIONS、ローカル実ブラウザー。

## I-016 県版全編由来の歩掛を全国標準として出典名だけ付け替えた

- 状態: **再現済み・修正済み・生成監査／固定値検証済み**
- 症状: 旧`consulting-standard-walks`の735表は県公開の年度全編から生成されていたが、県資料削除時に表値の由来を再検証せず、出典URLだけ国交省年度一覧へ置換していた。画面では原表ページ未対応としたものの、値の来歴を追跡できなかった。
- 原因: 「全国基準を採用した県版全編」と「国交省公式ページへ表値まで対応付けた全国マスター」を同一視した。
- 修正: 旧735表を現行画面の計算入力から外した。令和6～8年度統合表の各ページを国交省本体・累積改定へ逆照合し、対応した470／462／461行だけを新規則パックとして使用する。地質一般は編成人員を完成単価へ変換せず市場単価入力とする。
- 再発防止: 出典表示名の一括置換を禁止し、歩掛行ごとに元PDF、物理ページ、国交省URL、国交省ページ、照合確度、年度、経費体系を持たせる。低・未対応行は実行対象にしない。
- 証拠: `tools/build-consulting-fullbook-crosswalk.py`、`tools/build-consulting-rule-pack.py`、`data/source-audits/consulting-fullbook-crosswalk.json`、`data/consulting-rule-pack.json`、T-CONSULT-RULE-PACK。

## I-017 単位の既定分類と低確度表が実装範囲を過大表示する余地があった

- 状態: **再現済み・修正済み・固定値試験済み**
- 症状: 面積`m²`を整数の既定集合へ含め、個別メタデータがない項目は単位名によるフォールバックへ依存していた。また、年度統合版の低確度・未照合63ページは件数だけで、個別の計算可否が固定されていなかった。
- 原因: 入力制約とページ照合を全項目・全対象ページへ明示データとして持たせず、既定動作へ委ねていた。
- 修正: 全測量項目と全設計等歩掛へ入力ドメインを付与し、m²等を連続量とした。63ページは個別台帳化し、表あり15ページを含む全件を計算対象外に固定した。
- 再発防止: 生成スクリプト自体が入力ドメインを出力し、T-CONSULT-RULE-PACKとT-CROSSWALK-63で全件数を固定する。低確度資料を画面の数値選択・補正へ結線しない。
- 証拠: コミット`b929cc4`、`data/source-audits/input-domain-audit.json`、`data/source-audits/consulting-crosswalk-resolution.json`。

## I-018 実PDFで帳票の列切れと不要な継続ページが発生した

- 状態: **再現済み・修正済み・公開版実PDF目視検証済み**
- 症状: 初回の公開版PDFは、測量5帳票が7ページ、設計等3帳票が4ページになり、測量の単価・歩掛内訳書は右側列が印刷範囲外へ切れ、条件書と設計等内訳書には内容の少ない継続ページが生じた。
- 原因: 印刷CSSの名前付き横向きページがChromeの印刷経路で期待どおり適用されず、列幅とセル余白が印刷幅を超えた。注記と免責も改ページ境界を跨ぐ位置にあった。
- 修正: 全帳票をA4縦の固定ページへ揃え、単価・歩掛10列を比率幅・border-box・折返しで収めた。注記と免責を収まる帳票へ移し、表と注記の余白・文字寸法を調整した。
- 再発防止: 公開HTTPS版から匿名QAデータを投入する再現URLを保持し、帳票変更時は実PDF生成、全ページPNG化、目視確認を必須とする。ページ数だけで合格にせず、全列、表頭、金額、出典、免責、フッターを確認する。
- 証拠: コミット`1980c8b`、`5e52ae0`、`d63db5b`、`b901a2d`、測量5ページ・設計等3ページの最終QA PDF。

## I-019 HTML更新後に旧JavaScriptキャッシュが削除済み要素を参照した

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 症状: 測量の状態帯をHTMLから撤去した最初の公開版で、ブラウザーに残った旧`app.js?v=20260824-3`が`masterStatusTitle`を参照し、画面初期化と測量タブ操作で`TypeError`になった。
- 原因: `app.js`を変更したのに、`index.html`のスクリプト版番号を更新せず、HTMLとJavaScriptのキャッシュ世代が一致しなかった。
- 修正: `app.js`の版番号を`20260824-4`へ更新した。新規公開タブで新スクリプトの読込み、測量状態帯0件、年度・項目表示、横あふれ0、警告・エラー0件を確認した。
- 再発防止: 公開JavaScriptまたはCSSを変更した場合は、対応する`index.html`のクエリー版番号を同じ変更で更新する。Pagesビルド後は既存タブではなく新規タブまたは明示キャッシュバスターで公開版を確認し、読込スクリプトURLとコンソールを検査する。
- 証拠: 初回Pagesビルド`1171006848`の公開ブラウザーログ、修正コミット`a945774ca36f6b83705180282eb98201ecaec9e3`、修正後Pagesビルド`1171009494`。

## I-020 共通クラスだけを確認し、4業務の上部UIを統一済みと判定した

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 症状: 4業務の画面骨格を統一済みと記録した後も、測量だけに業務基本情報7項目があり、設計等の見積提出先は選択欄ではなく固定文字、設計等だけに説明帯が残っていた。
- 原因: 上部5項目とカードの共通CSSクラス数だけを主に検査し、各タブの入力項目数、操作形式、状態帯数を要素単位で比較していなかった。
- 修正: 設計等へ同じ業務基本情報7項目と見積提出先選択を追加し、全業務の状態帯を撤去した。基本情報と見積提出先は同じ案件データへ同期する。
- 再発防止: UI統一の回帰試験は、各業務タブについて上部項目数、基本情報項目数、入力種別、カード数、状態帯数を個別に数える。共通クラスが存在するだけでは統一済みと判定しない。
- 証拠: 修正前コミット`33643d0fbe0d55f564b0708fbf3e7f9b8c4bddd8`、修正コミット`897614da64abaece6f59be2c6833508217194f13`、T-UI、公開版4タブの要素数比較。

## I-021 4業務で作業項目の探し方が異なり、未収録と誤認しやすかった

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 症状: 測量は「第2編 基準点測量」等の分類選択、設計等は自由文字検索が最初にあり、同じアプリ内で操作方法が違った。設計等で「解析」と入力して該当なしになるなど、表記を知らない利用者が未収録と誤認する余地があった。
- 原因: 正式な章・節・業務分類をそのまま初期UIに露出し、利用者が探すための短い索引語を業務横断で設計していなかった。
- 修正: 4業務とも年度マスター連動のキーワードボタンを先頭へ置き、正式分類・条件選択はその後に残した。名称検索は補助操作へ移し、0件のキーワードは表示しない。
- 再発防止: 各年度・業務で、表示キーワードの候補集合が重複なく全候補と一致することを監査する。UI統一は見た目だけでなく、初期操作、選択状態、0件時の挙動まで実ブラウザーで比較する。
- 証拠: コミット`1687111ad670b8fa03101fafa764e66f5817aad7`、T-UI、T-CONSULT-UI、令和6～8年度の全候補分割監査、GitHub Pagesビルド`1171080579`。

## I-022 キーワードを統一した後も作業選択欄の名称と配置が業務別に残った

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 症状: キーワード入口は4業務で統一されたが、測量は「積算基準の作業区分・作業項目・積算数量」、設計等は「基準書の項目分類・業務種類／適用条件」で、積算数量も下段に離れていた。数量欄の見出しだけでは単位を即座に判断できなかった。
- 原因: D-040では初期検索方法と正式分類の保持を検証したが、その後の選択欄について表示名、並び順、単位表示、追加ボタンを要素単位で比較していなかった。
- 修正: 4業務の通常入力を同じ表示名と横並びへ変更し、数量見出しへ作業項目の単位を表示した。地質市場単価方式は選択単位に連動させ、複合数量は単位別入力を維持した。
- 再発防止: UI統一試験は、上部・カード・キーワードだけでなく、作業区分、作業項目、積算数量、単位、追加ボタンの文字と順序を4業務で比較する。
- 証拠: コミット`aa2be68eacc51fe3f097789a296011c9245c2a48`、T-UI、T-CONSULT-UI、ローカル／公開実ブラウザー、GitHub Pagesビルド`1171120305`。

## I-023 数量欄だけの補助文で横並びの枠がずれた

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 症状: 設計等の積算数量欄だけに「業務数／標準1業務当り／整数のみ」等が表示され、数量入力枠と追加ボタンの上下位置がずれて見えた。
- 原因: 入力規則の説明を数量欄の`label`内へ`small`要素として置き、横並び行の他の入力欄と要素高を揃えていなかった。
- 修正: 可視の補助文を撤去し、入力制限は入力属性・正規化処理・`aria-description`へ保持した。
- 再発防止: T-CONSULT-UIで数量欄内の補助文が0件であることと、入力規則のアクセシビリティ情報が残ることを固定検査する。実ブラウザーでは数量入力と追加ボタンの上端・下端差を測る。
- 証拠: コミット`81be9cb92bd3704ee35dc6802f5240bf8aff08d5`、GitHub Pagesビルド`1171148519`、公開版の位置差0px・警告／エラー0件。

## I-024 PDF取込の未選択状態へ直前の項目・単位が残った

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 症状: PDF側で反映候補を閉じた後も、空の入力パネルへ直前の測量項目、数量、単位が残る場合があり、利用者が入力済みと誤認する余地があった。
- 原因: 空パネル表示時も選択肢再生成処理へ直前の`select`値を渡し、項目と単位を明示的に空へ戻していなかった。
- 修正: 空パネルの初期化完了後に、項目コード、数量、資料単位をすべて空文字へ戻し、数量換算表示も未選択状態へ更新する。PDF文字枠を選択・ドロップした後の自動候補入力は変更しない。
- 再発防止: T-DOC-UIで3値の明示初期化を固定検査し、公開版で取込画面を開いた直後のDOM値がすべて空文字であることを確認する。
- 証拠: コミット`2692aeaf6eb94ad3d02503f4241ff266a76c565a`、GitHub Pagesビルド`1171205263`、公開実ブラウザーの`item=''`・`quantity=''`・`unit=''`・警告／エラー0件。

## I-029 PDF行クリック後に番号1・2・3が推定値で埋まった

- 状態: **再現済み・修正済み・固定値／ローカル／公開実ブラウザー検証済み**
- 症状: 空の入力パネルは修正済みだったが、PDF行をクリックすると測量の番号1へ作業項目、番号2へ`1`、番号3へ`回`等が再び入った。設計等でも行文字列、職種、人工が推定入力される経路があり、利用者が確認済みの値と誤認する余地があった。
- 原因: I-024では空パネル表示経路だけを初期化した。PDF行クリックの`openManualMapper`と業務区分切替の`updateManualKind`は独立して数量・作業項目を推定し、選択肢生成時も先頭の項目・職種を暗黙選択していた。
- 修正: クリック時と業務区分切替時に4業務共通の空欄初期化を通す。測量は項目・数量・単位、設計等は作業項目・職種・人工へ空の選択肢を設け、明示選択または各緑枠へのドロップまで値を設定しない。
- 再発防止: T-DOC-UIでクリック処理に数量推定・原文転記がないこと、業務切替に項目自動照合がないこと、測量と設計等の全入力値を空へ戻すことを固定検査する。実ブラウザーでは未判定PDF行をクリック後、4業務区分を順に切り替えて全6入力値が空文字であることを確認する。
- 証拠: コミット`0811d14e7316218e39197d293a320db9ecd88613`、全18試験、GitHub Actions Pages実行`32703890354`。公開版`document-import.js?v=20260824-14`で、クリック後および設計・測量・調査計画・地質切替後の項目・数量／人工・単位／職種がすべて空文字。

## I-025 紹介動画が無音でPDF取込も実操作を示していなかった

- 状態: **再現済み・修正済み・映像／音声／公開実ブラウザー検証済み**
- 症状: 初版の紹介MP4は映像トラックだけで、PDF取込場面も静止画上の説明に留まり、実際にどこからどこへドラッグするかを確認できなかった。
- 原因: 動画生成処理がスクリーンショット列だけを書き出し、ナレーション、BGM、実ブラウザー操作の撮影工程を持っていなかった。
- 修正: 匿名合成PDFを作成し、実アプリへ読み込んで項目・数量・単位を個別ドラッグし、反映待ちへ追加する4場面を撮影した。日本語SAPIナレーションと生成スクリプト内のオリジナルBGMをAACへ合成した。
- 再発防止: 動画変更時は全フレーム復号、映像・音声ストリーム、実音量、主要場面静止画、公開版`readyState`を検査する。実案件・顧客データを撮影素材に使わない。
- 証拠: コミット`42966c68c9810a999e45db013ea39329bf376ec8`、MP4 49.75秒・H.264/AAC・平均音量-17.6dB・最大-0.8dB、Pagesビルド`1171261067`。

## I-026 PDF業務名が要確認のままで候補案内も「右側」としか示さなかった

- 状態: **再現済み・修正済み・固定値／実ブラウザー検証済み**
- 症状: 業務数量総括表の先頭に案件固有の「…業務」があっても業務名は自動入力されず、複数候補時の通知は「右側で選択」とだけ表示して操作枠を特定できなかった。
- 原因: ラベルなし見出しをすべて同じ中確度候補として扱い、案件固有見出しと一般費目名の自動入力境界がなかった。通知文もデスクトップ配置だけを前提にした。
- 修正: 文字抽出PDFの1ページ目先頭10行以内で、年度・案件語・末尾「業務」を満たす見出しだけを4業務共通の業務名へ自動入力する。OCRと一般費目名は確認候補のままにする。通知は「PDF横（狭い画面では下）の緑枠」と枠名を示す。
- 再発防止: T-DOC-IMPORTで案件見出しの採用と一般費目名の除外を固定し、T-DOC-UIで自動反映結線と通知文を検査する。
- 証拠: コミット`42966c68c9810a999e45db013ea39329bf376ec8`、匿名合成PDFの4タブ同値、実ドラッグ通知、全18試験、Pagesビルド`1171261067`。

## I-027 発行者情報が案件データ内だけにあり新規作成で消えた

- 状態: **再現済み・修正済み・固定値／ローカル／公開実ブラウザー検証済み**
- 症状: 提出用帳票には会社名、担当者、郵便番号、住所、電話番号、メール、インボイス登録番号の入力欄があったが、案件データの一部としてしか保存されず、「新規」で全項目が空へ戻っていた。
- 原因: 案件固有の宛名・見積条件と、複数案件で再利用する自社・発行者情報を同じ`report`初期値だけで管理していた。
- 修正: 7項目を専用プロフィールキーで同一ブラウザー内へ入力時自動保存し、新規帳票の初期値へ合成する。案件固有情報は空のままにし、旧案件情報はプロフィール未作成時だけ移行する。匿名QA帳票にはプロフィールを混入させない。
- 再発防止: T-UIで7項目、専用キー、読込・保存、新規合成、旧案件移行、匿名QA分離を固定検査する。実ブラウザーでは新規作成と再読込の両方でプロフィールだけが残り、宛名・業務名が空になることを確認する。
- 証拠: コミット`8bc0eca999d841d419032764e9ff7916713d8d79`、全18試験、ローカル新規／再読込、公開7項目表示、Pagesビルド`1171311721`。

## I-028 PDF反映先だけ旧式の汎用作業名を使い原文と作業名が混同された

- 状態: **再現済み・修正済み・固定値／ローカル／公開実ブラウザー検証済み**
- 症状: 設計等のPDF行をクリックすると、原文「業務着手時」が編集可能な内訳名へ転記され、反映先は「土木設計業務→業務計画」等の旧式候補だった。通常入力のキーワード、積算基準の作業区分、年度別作業項目と一致していなかった。
- 原因: PDF手動対応付けが`CONSULTING_MASTER.taskNames`を独自に列挙し、通常入力が使う`CONSULTING_RULE_PACK`とキーワード定義を参照していなかった。
- 修正: 原文を確認専用表示へ固定し、4業務区分を明示。設計・調査計画・地質は共用キーワード台帳と年度別規則パック、測量は通常入力のキーワード定義と年度マスターを使う。追加候補へ公式規則IDを保持する。
- 再発防止: T-DOC-UIで旧汎用作業名参照0件、原文固定表示、4業務順、共用台帳、規則ID保持、匿名デモPDF QA入口を固定検査する。公開版では4業務件数、設計作業区分、公式作業項目、原文不変、コンソールを確認する。
- 証拠: コミット`c68e67738386d18c97cbb21e56fed3254412f34d`、全18試験、GitHub Pagesビルド`1171344364`。公開版の設計313・測量141・調査計画117・地質40項目、設計38作業区分、公式「業務着手時」、ブラウザーエラー0件。

## I-2026-08-24-UNIT-01 単位定義が3エンジンへ分散していた

- 状態: **再現済み・修正済み**
- 症状: 通常入力では整数単位として扱える単位でも、PDFの単位選択肢・別業務の数量式では認識範囲が異なり、`枚`の標準数量単位や`筆`等の資料単位を一貫して扱えなかった。
- 原因: `engine.js`、`consulting-engine.js`、`document-import-engine.js`が別々の整数集合、別名、正規表現、換算候補を保持していた。PDF側の標準数量単位は面積・延長だけに限定されていた。
- 修正: 41単位の共通台帳へ統合し、全エンジンを接続した。標準数量は単位種類を問わず選択可能にし、互換性のない換算は値を作らず追加を拒否する。
- 回帰防止: T-UNIT-CATALOGで3年度・4業務の未知単位0件、整数桁、標準100枚、筆の認識と換算拒否を固定する。

## I-2026-08-24-PDF-PENDING-01 項目を特定できても数量・単位不足で追加できなかった

- 状態: **再現済み・修正検証済み**
- 現象: PDFの作業項目を正しく選択しても、測量は数量と単位、設計等は職種と人工が揃わない限り「反映待ちへ追加」を実行できず、先に項目だけを拾う作業ができなかった。
- 危険: 未入力値を便宜的に1や標準数量へ補うと、利用者が入力済みと誤認し金額へ混入する。
- 修正: 入力待ち状態を明示的に保存して計算対象外とし、内訳画面で不足値を後入力できるようにした。標準単位内の数字は数量へ誤転記しない。
- 証拠: 実装コミット`9f024e1ef759e1ec340bfcd467d9054f10bf0a47`、全19試験、ローカルで測量0円・設計0円の入力待ち表示、後入力後の設計税込184,589円、公開版の数量・単位空欄と計算対象外カード、警告・エラー0件。

## I-2026-08-24-UI-UNIFICATION-01 測量だけ規定書表が項目選択直下に残った

- 状態: **再現済み・修正検証済み**
- 現象: 設計・調査計画・地質の計算根拠は画面下部の独立カードだったが、測量だけ項目選択欄の直下へ大きな規定書表を表示し、4業務の主要カード順が一致していなかった。
- 原因: I-011で「測量項目だけに使用規定書・URLを表示する」という対象範囲を守るため、測量専用表を項目直下へ実装した。その後の4業務UI統一では、項目・内訳・費用・集計の共通化だけを検査し、既存の測量専用表の配置を比較対象へ含めなかった。
- 修正: 規定書表の内容と項目連動は維持したまま、測量画面下部の「計算根拠」カードへ移した。4業務すべてで主要カードの見出しと順序を実DOMで比較した。
- 再発防止: 「4業務UI統一」の試験では、入力部品だけでなく、`workspace-main`直下の全カード数・順序・見出し・専用要素の位置を4業務で照合する。個別要望で専用表示を追加した場合も、次回の全体統一監査から除外しない。
- 証拠: 実装コミット`6f8bb690cfbf7b0955d72dd160d945606fcee91c`、T-UI、公開版の4業務でカード0～3が作業追加・積算内訳・追加費用・計算根拠の順であることを確認。

## I-2026-08-24-UI-UNIFICATION-02 測量のカード内部だけ旧書式が残った

- 状態: **再現済み・修正検証済み**
- 現象: 主要カードの順序は4業務で一致したが、測量だけ作業追加カード上部に長い説明帯があり、作業選択に旧`picker-grid`、計算根拠に3列表を使用していた。設計・調査計画・地質は説明帯なし、`verified-preset-row`、`source-list`だった。
- 原因: 前回の統一試験が`workspace-main`直下のカード順だけを検査し、各カード内部の子要素、共通クラス、表示部品、実寸を比較していなかった。
- 修正: 測量の説明帯を撤去し、設計等と同じ順序・共通クラスの作業選択グリッドへ置換した。測量の根拠4件は情報を削除せず、設計等と同じ`source-list`書式へ変更した。
- 再発防止: T-UI-UNIFICATIONで共通グリッド2件、作業区分欄2件、作業項目欄2件、測量説明帯0件、測量根拠表0件・根拠リスト1件を固定検査する。実ブラウザーでは設計と測量の列幅・追加ボタン高を比較する。
- 証拠: 実装コミット`cc739952c9034bbca2aca8ac6bc167b3e53f43a4`、全19試験、ローカル／公開実ブラウザー、GitHub Pagesビルド`1171664583`。

## I-2026-08-24-UI-UNIFICATION-03 測量の条件・補正が追加前に表示されず操作手順が異なった

- 状態: **再現済み・修正検証済み**
- 現象: 設計等は作業追加前に適用範囲・条件表・補正式を表示していたが、測量は数量から直接追加でき、補正選択は追加後の明細側にあった。また設計等だけに内容を確認したというチェックがあり、4業務の操作手順が一致していなかった。
- 原因: 前回は作業選択欄の外観と計算根拠書式を共通化したが、追加可否を決める検証処理と条件入力の表示時点を比較対象に含めなかった。
- 修正: 測量固有の適用範囲・条件表・補正を追加前の共通位置へ移し、全業務の確認チェックを撤去した。必須数量・条件・適用範囲を自動検証し、未入力時は明細へ追加せず不足理由を表示する。
- 再発防止: T-UI-UNIFICATIONで部品の見た目だけでなく、追加前の子要素順、確認チェック0件、自動検証関数、未入力時の明細非追加、条件選択の明細保存を検査する。
- 証拠: 実装コミット`ba86c5e3574edd26ebfdca40eb6bed34d58387e1`、全19試験、ローカル4業務比較、公開版測量の条件選択前後、GitHub Pagesビルド`1171689307`。

## I-2026-08-24-PDF-STICKY-01 右入力欄の確定ボタンが入力項目の下へ隠れた

- 状態: **再現済み・修正検証済み**
- 現象: PDFから項目・数量・単位を対応付ける右入力欄で、キーワードや入力項目が縦に長いと「反映待ちへ追加」「この行は対象外」がスクロール末尾へ隠れ、PDFを見ながらすぐ確定できなかった。
- 原因: 右サイドバー全体を1つのスクロール領域にし、確定ボタンを入力項目の末尾へ通常配置していた。
- 修正履歴: 当初は右入力欄を見出し、スクロール本文、固定フッターの3段へ分けた。再確認で操作が見つけにくい状態が残ったため、コミット`b5e6fb9`で操作2ボタンを見出し直下へ移した。確認事項はファイル投下欄の右横へ小型表示した。
- 再発防止: T-DOC-UIで操作欄がスクロール本文より前にあること、投下欄横の小型確認カード、反映先とキーワード欄の段分離を固定検査する。実ブラウザーでは最初に操作2ボタンが見え、作業項目だけで反映待ち1件へ追加できることを確認する。
- 証拠: 実装コミット`e7ec94be765b46e94751a1f08021ddaa7c336382`、全19試験、ローカル／公開匿名デモPDF、GitHub Pagesビルド`1171724182`。

## I-2026-08-24-MISSING-INPUT-01 未入力時に追加ボタンが押せず理由へ到達できなかった

- 状態: **再現済み・修正検証済み**
- 現象: 数量や条件表が未入力のとき、追加ボタン自体が`disabled`になった。理由文は画面下に表示されていたが、利用者は追加を押して確認できず、なぜ押せないか分かりにくかった。
- 原因: 入力検証結果を誤追加防止とボタン無効化の両方へ使い、未入力理由をクリック時に案内する経路がなかった。
- 修正: ボタンはクリック可能なままにし、未入力なら理由を警告表示して最初の不足欄へ移動・フォーカス・強調する。明細追加処理は検証成功時だけ実行する。
- 再発防止: T-UIとT-CONSULT-UIで4業務の追加ボタンを`disabled`にしないこと、未入力理由、対象欄セレクター、警告・フォーカス処理を固定検査する。公開実ブラウザーで警告、対象欄の強調、明細非追加を確認する。
- 証拠: 実装コミット`527a01b6ef18ede5b84c1dc2c1c2f51ec18369f1`、全19試験、GitHub Pagesビルド`1171742371`。

## I-2026-08-24-PDF-HEIGHT-01 PDF対応付けの見出し枠が縦作業領域を狭めた

- 状態: **再現済み・修正検証済み**
- 現象: PDF対応付け画面の上部に見出し、説明、ファイル名帯が約120pxを占有し、PDFと右入力欄は`78vh`・最大760pxへ制限されていた。
- 修正: 見出し枠を撤去し、PDFと右入力欄へ共通の拡張高さを適用した。1280×720の確認環境では両側が約562pxから680pxへ広がった。
- 再発防止: T-DOC-UIで見出し・説明0件、共通高さ変数、PDFと右入力欄への同一高さ指定を固定検査する。実ブラウザーで上端・高さ・追加ボタン表示を測定する。
- 証拠: 実装コミット`92c452bae6d75f721708b9e3288c1c0e32ff8aee`、全19試験、GitHub Pagesビルド`1171819577`。

## I-2026-08-24-PDF-MAPPER-CLUTTER-01 PDF右入力欄に重複見出しと小さい文字が残った

- 状態: **再現済み・修正検証済み**
- 現象: 右入力欄に、追加可否の説明、選択欄と同義の見出し、未選択時の同一メッセージが重複表示され、主要文字も9～12pxで小さかった。
- 原因: 入力可否を説明する状態文と、HTMLの入力欄名をすべて可視表示し、狭いサイドバー用に文字を必要以上に縮小していた。
- 修正: 重複案内と未選択メッセージを非表示にし、入力欄名はスクリーンリーダー用に保持した。主要文字を11～14pxへ拡大し、換算結果は必要値が揃った場合だけ表示する。パネル高さは680pxのまま維持した。
- 再発防止: T-DOC-UIで不要表示の非表示属性、`sr-only`入力名、条件付き換算表示、主要文字サイズを固定検査し、実ブラウザーでパネル高さとコンソールを確認する。
- 証拠: 実装コミット`cd48aa04255b077f472ffd4038a5ed7f6fdeb844`、全19試験、GitHub Pagesビルド`1171835847`。

## I-2026-08-25-CONDITION-VALUE-01 同じ補正率の別条件へ継承された

- 状態: **再現済み・修正検証済み**
- 現象: 測量条件で「森林／丘陵地（+40%）」を選んだ後、同じ補正率`0.4`を持つ「市街地甲／平地（+40%）」へ後続項目が切り替わる場合があった。
- 原因: 条件の意味ではなく`select.value`の補正率だけを保存・再選択したため、同値の先頭候補が選ばれた。
- 修正: 条件ラベルと率を組にして保存し、復元時は一致した`option.index`を直接選択する。同じ率でも異なる条件を区別する。
- 再発防止: 条件継承では数値率だけを識別子にしない。意味ラベル、規則ID、選択肢位置等の安定した意味情報と数値を組にする。
- 証拠: 実装コミット`aa0937315fcf4c490134fc5fc324ae60e611aa7e`、T-UI、ローカル実ブラウザーで森林・丘陵地と交通量条件を別項目へ正しく継承した。

### 追加済み明細での再発と修正

- 状態: **再現済み・修正済み・保存復元／公開検証済み**
- 再発現象: 現地測量`9-1-2`で追加前に「1/500 都市近郊／平地（+20%）」を選んでも、追加済み明細は同じ補正率の先頭候補「1/200 耕地／平地（+20%）」へ変わった。明細側で1/500へ変更しても再描画時に1/200へ戻った。
- 原因: 追加前の継承値はラベルと率の組へ修正済みだったが、明細は`correctionSelections`へ率だけを保存し、再描画で同率の先頭候補を選んでいた。
- 修正: 測量明細は率と意味ラベルを別々に保存し、再描画ではラベル一致を最優先する。追加前の現在値を明細へ固定し、追加後の手動変更を明細と後続作業の継承値へ同時反映する。設計・調査計画・地質の共通条件処理も値とラベルの組で保存し、追加直前の画面値と条件スナップショットを優先する。旧保存データは率または値によるフォールバックで読める。
- 再発防止: 同率条件の追加前、追加後変更、後続継承、保存復元をT-UI／T-CONSULT-UIと実ブラウザーで確認する。
- 証拠: 実装コミット`694a068b4f7e58ab3a0fb013ebe76bebe2e35269`、全19試験、ローカル実ブラウザーで1/500追加・明細変更・後続継承・保存復元、設計の同率「市街地／山地」識別、GitHub Pagesビルド`1173110384`。

### 数値式条件・汎用補正の追加監査

- 状態: **再現済み・修正済み・公開実ブラウザー検証済み**
- 監査結果: 選択式の条件は手動優先になっていたが、測量の水面幅等の数値式条件と、設計等の汎用補正チェックは同じ継承処理へ未接続だった。
- 修正: 測量の数値式条件は意味ラベルと単位をキーに業務内へ保存し、追加前と追加済み明細の手動変更を後続項目へ継承する。設計・調査計画・地質の共通処理は汎用補正を意味ラベルで保存し、手動チェック変更を次項目へ継承する。業務区分別の記憶分離と新規積算での初期化は維持する。
- 証拠: 実装コミット`9cec4192ce97b5af901416a2847f84cf40e041f1`、全19試験、ローカルで水面幅88→次項目88、追加済み明細99→次項目99、共同溝の汎用補正チェック復元、公開版で水面幅77の後続継承、Pagesビルド`1173165551`。

## I-2026-08-25-MOBILE-SURVEY-TABLE-01 スマホで測量明細の補正条件が画面外へ隠れた

- 状態: **再現済み・修正検証済み**
- 現象: スマートフォンの測量積算内訳で、補正条件の選択欄が「1/5…」等の先頭しか見えず、縮尺・地域・地形を確認できなかった。
- 原因: 条件選択欄自体は約288pxあったが、7列・約1,237pxの横長表の3列目に配置され、選択欄の大半が390pxの画面外へ出ていた。選択欄だけの幅調整では解消しない構造だった。
- 修正: 720px以下では測量内訳を1明細1カードへ変更し、条件・変化率欄をカード全幅へ配置した。補正条件の選択欄は`width: 100%`、`max-width: none`、`min-width: 0`とし、横スクロール表を解除した。
- 再発防止: T-UIで測量表のスマホ専用クラス、カード化、条件列の全幅配置、補正選択欄の幅指定を固定検査する。
- 証拠: 実装コミット`84df6fe02f1c30d68466e469e870606ea860d568`、全19試験、公開HTTP 200、配信HTML・CSSのカード化規則、GitHub Pagesビルド`1173224161`。

## I-2026-08-25-WORK-QUANTITY-DEFAULT-01 作業項目変更後も積算数量1が残った

- 状態: **再現済み・修正検証済み**
- 現象: 測量の追加前数量が初期値1で始まり、作業項目を変更しても標準数量または追加済み明細の旧数量が残った。未入力なのに入力済みと誤認できる状態だった。
- 原因: HTMLに`value="1"`があり、項目選択処理が`standardQuantity`を再設定していた。追加済み明細の項目変更処理も旧数量を新項目の桁規則で正規化して保持していた。
- 修正: 追加前数量を空欄開始にし、作業項目・年度マスターが変わった場合だけ空欄へ戻す。追加成功後も入力欄を空にする。追加済み測量明細の項目変更では数量を`null`にし、数量未入力・計算対象外へ戻す。設計・調査計画・地質の数量欄も項目変更後に空欄であることを確認した。
- 再発防止: T-UIとT-CONSULT-UIで4業務の数量自動補完禁止、測量明細変更時の計算除外を固定検査する。
- 証拠: 実装コミット`986c3f7103b48c9ba53fc74bcd4f47d5d0c2a2dc`、全19試験、ローカル実画面4業務、追加済み測量明細、公開HTML、ブラウザー警告・エラー0件、GitHub Pagesビルド`1173252333`。
